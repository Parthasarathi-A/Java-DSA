public class Solution {
    public int solve(int[] A) {
        int ans = 0;
        int n = A.length;
        Arrays.sort(A);
        for (int i = 0; i < n; i++) {
            if(A[i]==i){
                ans++;
            }
        }
        return ans;
    }
}
