public class Solution {
    public long subarraySum(int[] A) {
        int N = A.length;
        long total = 0;
        for(int i = 0;i<N;i++){
            total +=(long) A[i]*(i+1)*(N-i);
        }
        return total;
    }
}
